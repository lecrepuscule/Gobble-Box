<%= form_for [@user, @intake] do |f| %>

  <%= f.collection_select(:dish_id, Dish.all, :id, :name, {selected: @dish.id}) %>

  <%= f.label :serving %>
  <%= f.number_field :serving %>

  <% f.label :date %>
  <%= f.date_select :date %>

  <%= f.submit value: "Confirm" %>

<% end %>






