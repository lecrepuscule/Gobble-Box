<h1>Why not try: </h1>

<div class="row">
  <% @dishes["hits"].each do |dish| %>
  
    <div class="small-4 large-4 columns">
      <%= image_tag dish["recipe"]["image"] %>
      <%= link_to dish["recipe"]["label"], dish["recipe"]["url"] %>
    </div>

  <% end %>
</div>